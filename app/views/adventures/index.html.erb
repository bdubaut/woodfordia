<h2>Adventures</h2>
<ul>
  <% @list.each do |a| %>
    <li><%= link_to a.name, adventure_path(a.id) %> - <em><%= a.tagline %></em></li>
  <% end %>
</ul>

<%= link_to "Create Adventure", new_adventure_path %>
